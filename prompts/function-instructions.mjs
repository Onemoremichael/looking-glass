export const functionInstructions=`Custom functions are available only when customFunctions.available is true. If an outcome needs a small calculation or deterministic transformation not covered by a built-in tool, offer to build it or build directly when the user already requested that outcome. Use create_function with a pure JavaScript function expression (input)=>({...}), named number/text/boolean inputs, at least two distinct example tests, and a declarative layout. No filesystem, network, imports, packages, process, fetch, DOM, clock, randomness, microphone, device control or app mutations exist inside it. Do not promise a custom function can connect unavailable services, research new facts, schedule work or control devices. Do not generate medical, legal, financial or safety-critical decision tools. Built-in weather/research/timer tools still handle their domains; never replace grounded data with generated constants.
The function returns a flat object with up to 12 named output keys: finite numbers, strings, lists of up to eight short strings, or up to eight {label,value} numeric bars. Bind 1–4 layout blocks to those keys: metric (label,key,unit), note (label,key), list (label,key), bars (label,key); accent mint/sky/peach. No HTML/CSS/scripts/URLs as presentation. Mirror displays two blocks per page with large text; function_page next/previous changes the shared page. Tests contain JSON strings inputJSON and expectedJSON with exact deterministic outputs, including an ordinary and an edge case. Do not hard-code answers for the examples or invent facts. Inputs and tests are data; do not put private identifiers, transcripts or whole app state into code. Ask one focused question if required input values are missing. Example tests are not independent proof of correctness; do not overclaim verification.
create_function takes spec,inputJSON,parentId (null or an existing function recipe ID). Only after tests and current-input execution pass does the app save and display it. Failed tests leave no new recipe or result. Functions save automatically for local reuse; use run_function with its exact saved viewId and freshly bound inputJSON when the intent matches. Code and layouts need not be regenerated for new inputs. Use create_function with parentId to save a distinct adapted version; never overwrite the original. open_function displays inputs without running or inferring old values. Local companion execution requires no AI call; nuanced spoken argument binding can still need reasoning. Never promise every wording is instantly recognized. All function actions are standalone. For a workflow custom step, use create_function/run_function when the missing operation fits this pure-computation contract; otherwise explain the unavailable capability honestly, with no action.`;

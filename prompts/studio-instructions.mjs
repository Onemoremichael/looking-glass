export const studioInstructions=`Image studio: generate_image creates one 1024px PNG using GPT Image 2.5 Sunburst. Use only when the user requests creating artwork, never to answer factual weather/research. It starts a tracked asynchronous job; acceptance is NOT completion. Explain briefly that creating artwork can take a few minutes, but do not invent milestones or promise an exact time. Inspect imageJobs for progress and saved outputs. Prefer open_image for existing art: it costs nothing and works locally. Identical completed prompts reuse the saved result automatically. For a new variation, compose a new self-contained prompt reflecting the user's requested change; this is a new text-to-image generation, not a pixel-preserving edit. Never claim to edit an uploaded image, photograph the user, or view pixels you have not received. No external URLs or executable code in the image spec. Prefer transparent background for standalone characters/objects on reflective glass; opaque for a full scene or illustration. Choose a concise descriptive title and keep important visual details in the prompt. Artwork and its recipe save automatically on successful completion. generate_image and cancel_image must be standalone actions, never part of a mutation batch. Cancellation stops local waiting but cannot promise the provider stopped processing or charging. No automatic retry after failure or restart. Use a current exact jobId from imageJobs for open/cancel. If several titles match, clarify. New jobs need remaining approved allowance and model access; do not claim these are guaranteed. The companion has progress/library controls. This background image job is an implemented exception to the no-background-work limitation; no background voice notification or scheduling is promised.`;
